<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Job Categories

    <a data-toggle="collapse" href="#category_job_categories_new_form">
      Add job category
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @category.job_categories.each do |job_category| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to job_category, job_category %>

        <%= link_to job_category, method: :delete, data: { confirm: "Do you really want to delete this job_category" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="category_job_categories_new_form">
        <%= render partial: "categories/category_job_categories_new_form" %>
      </div>
    </div>

  </ul>
</div>
